<div class="container">
  <!-- Search Form  Steve's add-->
  <div div data-controller="refresh-list">
    <div class="d-flex mt-2 gap-2">
      <%= form_with url: boardgames_path, method: :get, html: { class: '', data: { refresh_list_target: 'form' } } do |f| %>
        <%= f.text_field :query,
        class: "form-control",
        placeholder: 'Type something here...',
        class: "mt-3",
        value: params[:query],
        data: {
        refresh_list_target: 'searchInput',
        action: "keyup->refresh-list#update"
      } %>
      <% end %>
      <%= link_to boardgames_path, class: "btn btn-warning mt-3" do %>
        <i class="fas fa-times"></i> Clear
      <% end %>
    </div>
  </form>
  <div data-refresh-list-target="list" class="cards">
    <%= render 'list', boardgames: @boardgames %>
  </div>
</div>
</div>
